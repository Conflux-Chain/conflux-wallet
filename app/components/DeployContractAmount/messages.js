/*
 * DeployContractAmount Messages
 *
 * This contains all the text for the DeployContractAmount component.
 */
import { defineMessages } from 'react-intl';

export const scope = 'app.components.DeployContractAmount';

export default defineMessages({
  amountTitle: {
    id: `${scope}.amountTitle`,
    defaultMessage: 'Amount',
  },
});
