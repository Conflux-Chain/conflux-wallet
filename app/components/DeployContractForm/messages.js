/*
 * DeployContractForm Messages
 *
 * This contains all the text for the DeployContractForm component.
 */
import { defineMessages } from 'react-intl';

export const scope = 'app.components.DeployContractForm';

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'Source',
  },
});
